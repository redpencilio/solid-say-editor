<ul ...attributes>
    {{#each @files as |file|}}

    {{#if file.children}}
    <li class="folder">
        {{file.name}}
        <FileList @files={{file.children}} class="hidden"/>
    </li>
    {{else}}
    <li>
        {{file.name}}
    </li>
    {{/if}}

    {{/each}}
</ul>