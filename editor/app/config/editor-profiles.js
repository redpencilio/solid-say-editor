export default {
  all: [
    "rdfa-editor-say-solid-plugin"
    // This list will contain the plugins you want to run for the 'all' profile.
  ],
   /*  An additional profile may be added,  e.g.
      adminUser: [
       // List of plugins useful for an administrator.
      ]
   */
 };