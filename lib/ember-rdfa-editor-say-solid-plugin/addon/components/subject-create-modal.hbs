<WuModal @title="Add Subject to " @dialog-class="modal-dialog--wide modal-dialog--sectioned">
    <p>{{@graph.value}}</p>
    <div class="modal-dialog__footer">
        <label class="form__label form__label--block u-spacer--small" for="predicate">
            Subject name
            <Textarea class="textarea textarea--block u-spacer--tiny" rows="1" @value={{this.subjectValue}} />
        </label>
        <label class="form__label form__label--block u-spacer--small" for="object"> Object:
            <Textarea class="textarea textarea--block u-spacer--tiny" rows="1" @value={{this.typeValue}}/>
        </label>
            
    <WuButtonGroup>
        <WuButton  @label="Save" class="button button--narrow" @onClick={{this.save}}/>
        <WuButton @label="Cancel" class="button button--alt button--narrow" @onClick={{@onClose}}/>
    </WuButtonGroup>
  </div>
</WuModal>